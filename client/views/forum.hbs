
<div class="jumbotron" style=''>
  <h1>BreakMC Forums</h1>
  <p>Interact with the staff of the BreakMC network, suggest new ideas and more on the new BreakMC Forums. Make sure to Register in order to post threads! Here you can select a category to browse.</p>
</div>


<table class="table table-forum">
  <tbody>
    {{#each categories}}
      <!--Thread-->
      <tr>
        <!--Icon-->
        <td class="table-forum--icon">
          <img src="/assets/{{icon}}" alt="{{title}}" />
        </td>
        <!--Thread-->
        <td class="table-forum--title">
          <a href="/forum/category/{{title}}" class="thread-title">{{title}}</a>
          <span class="ago clearfix">{{description}}</span>
        </td>
        <!--No. of Replies-->
        <td class="table-forum--replies" style='display: none'>
          <span class="count">{{replies.length}}</span>
          <span class="light clearfix">Replies</span>
        </td>
        <!--No. of Views-->
        <td class="table-forum--views" style='display: none'>
          <span class="count">{{views}}</span>
          <span class="light clearfix">Views</span>
        </td>
        <!--Latest Reply-->
        <td class="table-forum--latest" style='display: none'>
          <a href="/post/{{id}}">
            <img src="https://minotar.net/avatar/{{user}}" alt="Luca" class="icon" />
            <span class="time">{{time}}</span>
          </a>
          <p>{{snippet}}</p>
        </td>
      </tr>
      <!--Thread-->
    {{/each}}
  </tbody>
</table>

{{#if auth_bool}}
  <a href='/forum/new'><button type="button" class="btn btn-info newThread">New Thread</button></a>
{{/if}}