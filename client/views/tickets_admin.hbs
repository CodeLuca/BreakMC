
<div class="jumbotron" style=''>
  <h1>BreakMC Tickets</h1>
  <p>Reply to people's tickets admins. Make sure to give detailed replies and solve their tickets as soon as possible!</p>
</div>

<table class="table table-forum tickets">
  {{#unless tickets}}
    <h2>There are no tickets here yet...</h2>
  {{/unless}}
  <tbody>
    {{#each tickets}}
      <!--Thread-->
      <tr>
        <div class='testDiv'>
          <!--Icon-->
          <td class="table-forum--icon">
            <img src="https://minotar.net/avatar/{{user}}" alt="Luca" />
          </td>

          <!--Thread-->
          <td class="table-forum--title forum_cat">
            <a href="/forum/view/{{id}}" class="thread-title">{{title}}</a>
            <span class="ago clearfix">{{user}}</span>
          </td>
        </div>
        <!--No. of Replies-->
        <td class="table-forum--replies">
          <span class="count">{{replies.length}}</span>
          <span class="light clearfix">Replies</span>
        </td>
      <!--Thread-->
    {{/each}}
  </tbody>
</table>